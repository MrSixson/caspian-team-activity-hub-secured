# وضعیت امنیتی سامانه Caspian

این سند توضیح می‌دهد چه چیزهایی اصلاح شد، چه کاری باید همین امروز انجام بدهید، و چه چیزی هنوز باقی مانده است.

---

## ۱. کاری که باید فوراً انجام دهید

رمز عبور `aminjodaie314@gmail.com` قبلاً داخل کد فرانت‌اند بود. یعنی هر کسی که سورس سایت را باز کرده، آن را دیده است. **فرض کنید لو رفته است.**

1. اگر از این رمز در جای دیگری (ایمیل، شبکه اجتماعی، دیسکورد، بانک) استفاده کرده‌اید، همان‌جا هم عوضش کنید. این مهم‌ترین قدم است.
2. مقدار `SEED_OWNER_PASSWORD` را در فایل `.env` با یک رمز جدید و منحصربه‌فرد تنظیم کنید.
3. اگر این پروژه روی گیت‌هاب یا هر مخزن عمومی است، رمز قدیمی هنوز در تاریخچه‌ی کامیت‌ها باقی می‌ماند حتی بعد از این تغییرات. یا مخزن را خصوصی کنید یا تاریخچه را پاک‌سازی کنید (`git filter-repo`).
4. کلید `GEMINI_API_KEY` را هم اگر جایی کامیت شده، از کنسول گوگل باطل و جایگزین کنید.

---

## ۲. موارد رفع‌شده

### ۲.۱ احراز هویت بک‌اند (مورد ۱ گزارش)

قبلاً هر مسیری در `server.ts` بدون هیچ بررسی‌ای در دسترس همه بود.

اکنون:

- ورود موفق یک **توکن نشست** تولید می‌کند (۳۲ بایت تصادفی). سرور فقط هش SHA-256 آن را نگه می‌دارد، پس حتی لو رفتن حافظه یا لاگ‌ها توکن قابل استفاده نمی‌دهد.
- توکن با هدر `Authorization: Bearer` فرستاده می‌شود، نه کوکی؛ بنابراین API در برابر CSRF مصون است.
- هر مسیر یکی از نگهبان‌های `requireAuth`، `requireAbility(...)` یا `requireFounder` را دارد.
- `/api/system/reset`، `/api/system/backup` و `/api/system/restore` فقط برای فاندر.
- تعلیق، حذف، تنزل رنک و بازنشانی رمز، بلافاصله تمام نشست‌های آن کاربر را باطل می‌کند.
- سقف تلاش ورود: ۲۰ بار در ۱۵ دقیقه برای هر IP و ۸ بار برای هر نام کاربری.
- مسیرهای هوش مصنوعی هم نیاز به ورود دارند (سهمیه ۳۰ درخواست در ساعت)، تا کلید Gemini شما توسط افراد ناشناس مصرف نشود.

**قانون سلسله‌مراتب:** هیچ‌کس نمی‌تواند روی کاربری با رنک مساوی یا بالاتر از خودش عمل کند، رنکی مساوی یا بالاتر از رنک خودش اعطا کند، یا رنک خودش را تغییر دهد. رنک `Founder` فقط توسط فاندر قابل اعطاست و آخرین فاندر فعال قابل حذف یا تعلیق نیست.

### ۲.۲ رمزهای متن ساده (مورد ۲)

- رمزها با **scrypt** (پارامترهای `N=16384, r=8, p=1`) و نمک اختصاصی هش می‌شوند. از `crypto` داخلی Node استفاده شده، پس وابستگی جدیدی اضافه نشد.
- مقایسه با `timingSafeEqual` انجام می‌شود.
- هیچ رمزی در `initialData.ts`، `storage.ts` یا هیچ فایل فرانت‌اندی باقی نمانده است. فیلد `password` از مدل `User` سمت کلاینت **حذف** شده تا کامپایلر جلوی بازگشت آن را بگیرد.
- رمزهای اولیه‌ی سه حساب اصلی از متغیرهای محیطی خوانده می‌شوند. اگر تنظیم نشده باشند، سرور یک رمز تصادفی قوی می‌سازد و **یک بار** در کنسول چاپ می‌کند.
- سیاست رمز: حداقل ۸ کاراکتر، شامل حداقل یک حرف و یک عدد (هم سمت سرور، هم در فرم‌ها).

**مهاجرت خودکار:** اگر فایل `data_store.json` قبلی شما رمزهای متن ساده دارد، در اولین اجرا هش می‌شوند، فیلد متن ساده حذف می‌شود، و آن حساب‌ها با پرچم «تغییر اجباری رمز» علامت می‌خورند. هیچ کاری لازم نیست انجام دهید.

### ۲.۳ نشت رمزها از `/api/users` (مورد ۳)

- تابع `toPublicUser` هر فیلد اعتبارسنجی را قبل از خروج از پروسه حذف می‌کند. تست‌ها تأیید می‌کنند که هش در خروجی JSON ظاهر نمی‌شود.
- این مسیر اکنون نیاز به ورود دارد و ایمیل سایر کاربران فقط برای مدیران و خود شخص نمایش داده می‌شود.
- `/api/system/backup` هم هش‌ها را از خروجی حذف می‌کند.
- `/api/system/restore` دیگر نمی‌تواند رمز یا هش تزریق کند یا حساب جدید بسازد؛ فقط فیلدهای غیراعتبارسنجی حساب‌های موجود را بازیابی می‌کند.

### ۲.۴ ثبت‌نام آسیب‌پذیر (مورد ۴)

- فیلدهای `role`، `permissionLevel`، `status`، `isFounder` و `customPermissions` از بدنه‌ی درخواست کاملاً نادیده گرفته می‌شوند.
- هر ثبت‌نام جدید با رنک `Trial Helper`، سطح `User` و وضعیت `Pending` ساخته می‌شود و تا تایید مدیریت اجازه‌ی ورود ندارد.
- سقف ۵ ثبت‌نام در ساعت برای هر IP.

### ۲.۵ CORS باز (مورد ۵)

- `Access-Control-Allow-Origin: *` حذف شد.
- فقط دامنه‌های فهرست‌شده در `ALLOWED_ORIGINS` مجازند. اگر خالی باشد، هیچ هدر CORS ارسال نمی‌شود و فقط درخواست‌های هم‌مبدأ کار می‌کنند.
- هدرهای `X-Content-Type-Options`، `X-Frame-Options: DENY`، `Referrer-Policy`، `Cross-Origin-Opener-Policy` و `Permissions-Policy` اضافه شدند و `x-powered-by` غیرفعال شد.

### ۲.۶ دسترسی حساب امین جدایی (مورد ۶)

دسترسی کامل Owner با تمام `customPermissions` حفظ شده است — هم در `initialData.ts` و هم در `server/seed.ts`. تنها چیزی که حذف شد، رمز عبور بود.

### ۲.۷ مواردی که در بررسی اولیه نبود ولی پیدا شد

| مورد | توضیح |
|---|---|
| ورود بدون رمز | دکمه‌های «ورود سریع کادر مدیریت» با `onSelectUserDirect` بدون هیچ رمزی وارد حساب فاندر می‌شدند. کل این مسیر حذف شد. |
| تعویض کاربر | `onSwitchUser` در نوار بالا اجازه می‌داد بدون رمز به حساب دیگری سوییچ کنید. حالا فقط خروج از حساب انجام می‌دهد. |
| اعلان رمز | بازنشانی رمز، مقدار جدید را داخل متن اعلان ذخیره می‌کرد. اکنون رمز هیچ‌جا بازتاب داده نمی‌شود. |
| رمز پیش‌فرض `123456` | مقدار پیش‌فرض ساخت حساب توسط ادمین حذف شد. |
| یادداشت‌های محرمانه | `isInternalNote` در تیکت‌ها به همه برمی‌گشت. حالا فقط استف می‌بیند. |
| جعل هویت در تیکت | `senderId` و `isStaffReply` از بدنه‌ی درخواست خوانده می‌شد. حالا از نشست گرفته می‌شود. |
| جعل گزارش کار | `userId` و `status` گزارش از کلاینت می‌آمد. حالا سرور تعیین می‌کند. |
| RSVP جلسات | `userId` از بدنه خوانده می‌شد، پس می‌شد از طرف دیگران ثبت حضور کرد. |
| دسترسی تیکت‌ها | هر کسی همه‌ی تیکت‌ها را می‌دید. حالا فقط تیکت‌های خودش یا، اگر استف باشد، همه. |
| مجوز فایل دیتابیس | `data_store.json` با مجوز `0600` و به صورت اتمیک نوشته می‌شود و به `.gitignore` اضافه شد. |
| پیام خطای ورود | تفاوت پیام «کاربر یافت نشد» و «رمز اشتباه» اجازه‌ی شمارش حساب‌ها را می‌داد. حالا پیام یکسان است. |
| نشت خطای سرور | `err.message` به کلاینت برمی‌گشت. حالا فقط در لاگ سرور می‌ماند. |

---

## ۳. متغیرهای محیطی

فایل `.env.example` را به `.env` کپی کنید:

```bash
cp .env.example .env
```

| متغیر | توضیح |
|---|---|
| `ALLOWED_ORIGINS` | دامنه‌های مجاز، با کاما. خالی = فقط هم‌مبدأ. هرگز `*` نگذارید. |
| `TRUST_PROXY` | اگر پشت Cloud Run یا Nginx هستید `true`. |
| `SEED_FOUNDER_PASSWORD` | رمز اولیه‌ی بهنام بهرامیان. |
| `SEED_OWNER_PASSWORD` | رمز اولیه‌ی امین جدایی. |
| `SEED_SUPERVISOR_PASSWORD` | رمز اولیه‌ی امیرحسین جعفری. |
| `DATA_STORE_PATH` | مسیر فایل داده. خارج از پوشه‌ی سرو‌شده نگه دارید. |

---

## ۴. آنچه هنوز باقی مانده است

این نکته را جدی بگیرید:

**بخش عمده‌ی رابط کاربری هنوز `localStorage` را منبع حقیقت می‌داند.** احراز هویت، ثبت‌نام، ساخت حساب، تغییر رنک و بازنشانی رمز اکنون از سرور می‌گذرد، اما لیست گزارش‌ها، اخطارها، وظایف و جلسات هنوز در مرورگر نگهداری می‌شود.

یعنی کاربری که `localStorage` مرورگر خودش را دستکاری کند، می‌تواند ظاهرِ رابط کاربری خودش را عوض کند و مثلاً دکمه‌های مدیریتی را ببیند. **اما** هر عملیات واقعی وقتی به سرور برسد رد می‌شود، چون سرور به نشست و رنک واقعی نگاه می‌کند، نه به چیزی که مرورگر ادعا می‌کند.

برای رسیدن به وضعیت کاملاً امن، مسیر پیشنهادی به ترتیب اولویت:

1. `saveReports`، `saveWarnings`، `saveTasks`، `saveTickets` و `saveMeeting*` را با فراخوانی‌های `src/lib/api.ts` جایگزین کنید و `localStorage` را فقط به عنوان کش موقت نگه دارید.
2. `data_store.json` را با یک دیتابیس واقعی (SQLite یا Postgres) عوض کنید. فایل JSON در برابر نوشتن هم‌زمان مقاوم نیست.
3. نشست‌ها الان در حافظه‌ی پروسه‌اند؛ با ری‌استارت سرور همه خارج می‌شوند و روی چند اینستنس کار نمی‌کنند. اگر مقیاس گرفتید، Redis یا JWT امضاشده استفاده کنید.
4. لاگ ممیزی برای تغییر رنک، حذف کاربر و بازنشانی رمز اضافه کنید.
5. احراز هویت دو مرحله‌ای برای رنک‌های Founder و Owner.

---

## ۵. تست‌ها

منطق امنیتی با ۴۵ تست رفتاری بررسی شد: قالب و یکتایی هش، رد رمز اشتباه، رد مقادیر بدشکل، سیاست رمز، تولید و ابطال نشست، تجزیه‌ی هدر Bearer، محدودیت نرخ، ماتریس دسترسی‌ها، قوانین سلسله‌مراتب رنک، و اینکه خروجی JSON کاربر هیچ هشی را لو نمی‌دهد.

برای اجرای مجدد، ماژول‌های `server/security.ts` و `server/models.ts` وابستگی خارجی ندارند و مستقیم با Node قابل تست‌اند.
